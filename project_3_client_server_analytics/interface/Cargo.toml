[package]
name = "interface"
version = "0.1.0"
edition = "2024"

[lib]
name = "interface"
path = "src/lib.rs"

[dependencies]
tarpc = "0.37.0"
tokio = { version = "1.0", features = ["macros"] }
analytics_lib = { path = "../analytics_lib" }
